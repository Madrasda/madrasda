import {createTheme} from "@mui/material";

export const theme = createTheme({
	palette: {
		primary: {
			main: '#FFA000',
			dark: '#e18f00',
			contrastText: '#fff', // specify the contrast text color for the custom color
		},
	}

});
;
